VITE_API_URL=http://localhost:5000/api
# Optional — used to build optimized Cloudinary delivery URLs for advertisements.
# If omitted, the cloud name is inferred from each ad's secure_url.
VITE_CLOUDINARY_CLOUD_NAME=
